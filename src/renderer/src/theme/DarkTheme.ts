import {createTheme, Theme} from "@mui/material";

export const darkTheme: Theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#000000',
    }
  },
  typography: {
    fontFamily: 'cursive'
  }
})
